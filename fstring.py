# ython offers a powerful feature called f-strings (formatted string literals) to simplify string
# formatting and interpolation. f-strings is introduced in Python 3.6 it provides a concise and \
#     intuitive way to embed expressions and variables directly into strings.
# The idea behind f-strings is to make string interpolation simpler. 

val=19
print(f"value of your goods is{val}")